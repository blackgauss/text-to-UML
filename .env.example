# ── LLM Provider ──────────────────────────────────────────
# Supported: openai, ollama
LLM_PROVIDER=openai
OPENAI_API_KEY=
LLM_MODEL=gpt-4o

# Uncomment below to use Ollama instead:
# LLM_PROVIDER=ollama
# LLM_MODEL=llama3
# OLLAMA_BASE_URL=http://localhost:11434/v1

# ── Pipeline settings ────────────────────────────────────
# Available pipelines: default, fast
PIPELINE=default
MAX_RETRIES=3
DIAGRAM_TYPE=auto
SKIP_REFINE=false
